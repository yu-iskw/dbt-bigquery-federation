[project]
name = "dbt-bigquery-federation"
version = "0.1.0"
description = "dbt macros for pinned BigQuery EXTERNAL_QUERY federation"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pre-commit>=4.5.1",
]
