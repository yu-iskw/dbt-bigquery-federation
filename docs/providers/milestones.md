# Provider milestones

This milestone completes AlloyDB PostgreSQL conformance on the shared PostgreSQL-family planner and introduces the separate Spanner GoogleSQL provider profile for `EXTERNAL_QUERY`. Spanner external datasets remain out of scope.
