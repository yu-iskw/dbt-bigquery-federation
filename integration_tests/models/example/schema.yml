version: 2

models:
  - name: stg_users
    description: Example staging model used for GCP-free dbt build smoke tests
    columns:
      - name: id
        tests:
          - not_null
          - unique
      - name: name
        tests:
          - not_null
      - name: email
        tests:
          - not_null
