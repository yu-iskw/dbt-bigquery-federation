# Validation boundary

Every-PR tests validate provider descriptors, metadata SQL generation, type planning, and BigQuery SQL rendering. Real Connection API/IAM/network/runtime federation remains an authenticated GCP conformance layer.
